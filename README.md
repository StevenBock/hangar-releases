# Hangar — Releases

Public distribution channel for [Hangar](https://github.com/StevenBock/hangar) (source is private).

Each release contains signed macOS (Apple Silicon) and Windows builds plus `latest.json`, the feed used by the in-app auto-updater. Builds are produced automatically by CI on tagged releases.
